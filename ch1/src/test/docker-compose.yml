services:
  mongo_test:
    image: mongo:6.0.4
    ports:
      - "27017:27017"
    environment:
      DATABASE_URL: "mongodb://localhost:27017/blog"
    networks:
      - test_net

networks:
  test_net:
    driver: bridge
